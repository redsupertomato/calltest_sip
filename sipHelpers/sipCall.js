import React from 'react'
import { View, Text } from 'react-native';

const sipCall = (userAgent, ext) => {
  alert ('>>> starting sipCall to ' + ext);
  return (
    <div></div>
  )
}

export default sipCall